import request from '@/utils/request.js'

export const getClubComAPI=({pageNum,pageSize})=>{
    return request({
        url:'   ',
        params:{
          pageNum,
          pageSize

        }
      })
}